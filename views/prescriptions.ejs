<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Prescriptions - Prescripta</title>

    <!-- Bootstrap 5 CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">

    <!-- Font Awesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">

    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(to right, #007bff, #6610f2);
            color: white;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            padding: 20px;
        }

        .container {
            width: 90%;
            max-width: 800px;
        }

        .prescription-box {
            background: rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(10px);
            padding: 15px;
            border-radius: 10px;
            margin-bottom: 15px;
            box-shadow: 0px 8px 20px rgba(0, 0, 0, 0.2);
        }

        .prescription-box p, .prescription-box h6 {
            margin: 5px 0;
        }

        .prescription-box ul {
            padding-left: 20px;
        }

        .prescription-box ul li {
            list-style-type: none;
            padding: 5px 0;
        }

        .prescription-box ul li::before {
            content: "💊 ";
        }
    </style>
</head>
<body>

    <div class="container">
        <h2 class="text-center"><i class="fa fa-file-medical-alt"></i> All Prescriptions</h2>

        <% if (!prescriptions || prescriptions.length === 0) { %>
            <p class="text-warning text-center">No prescriptions found.</p>
        <% } else { %>
            <% prescriptions.forEach(prescription => { %>
                <div class="prescription-box">
                    <p><strong>🆔 Prescription ID:</strong> <%= prescription.fileId %></p>
                    <p><strong>👨‍⚕️ Doctor:</strong> <%= prescription.doctorName %></p>
                    <p><strong>🧑‍⚕️ Patient:</strong> <%= prescription.patientName %></p>
                    <p><strong>📜 Government Number:</strong> <%= prescription.govNumber %></p>
                    <p><strong>⚧ Gender:</strong> <%= prescription.gender %></p>
                    <p><strong>🎂 Age:</strong> <%= prescription.age %></p>
                    <h6><strong>Prescriptions:</strong></h6>
                    <ul>
                        <% prescription.prescriptions.forEach(p => { %>
                            <li><%= p.medicine %> - <strong>Dosage:</strong> <%= p.dosage %></li>
                        <% }) %>
                    </ul>
                </div>
            <% }); %>
        <% } %>
    </div>

    <!-- Bootstrap 5 JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
